
fn main() {
  // primitive types
  let message = "Hello world!";

  // boolean values
  let is_valid = true;

  // 2ˆ8 - 1 = 255
  // always the same formula
  // positive integer only
  let small_num:u8 = 255;

  // -2ˆ7 -> 2^7 = -128 -> 127
  // always the same formula, the number - 1
  // i can hold negative numbers
  let small_num_2: i8 = 127;

  // defined by operate system architecture, 32 or 64 bits
  let sys_num: isize = -10;
  let sys_num_2: usize = 10;
}